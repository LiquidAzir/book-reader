Original prompt: Apply the same visual/UI/control and bug-fixing overhaul to Book Reader, Hexland and Glass Pitch, using their latest Meta Display GitHub versions and the previous review-and-production-release process.

2026-09-15 backend pass: Kept the existing anonymous device identity, database schema and API payloads. Catalog and health no longer write to the personal-library database. Library async errors are caught under Express4; database outages return retryable503 instead of invalid-identity401. Strict fraction validation prevents null/false/string coercion from resetting progress to zero. Personal responses are private/no-store; metadata/IDs have bounded types and sizes.

Catalog and text loading now deduplicate simultaneous fetches, cap total cache memory and concurrent upstream requests, enforce timeouts through streamed bodies, validate Gutenberg URLs and every redirect, reject HTML/error pages as books, and preserve404 for missing editions. Dependency patches remove the three reported moderate advisories without upgrading to a new major framework.

Verification: server npm test passes14 focused scenarios (15 Node results including parent), with isolated database/upstream fixtures; covers user scoping, retries/error survival, malformed and oversized bodies, progress and IDs, redirects, concurrent readers, book-content validation, response limits and post-header stream timeout. npm audit reports0 vulnerabilities. Real catalog/text smoke succeeded for Pride and Prejudice and Alice; Gutenberg's canonical redirect is upgraded to HTTPS without following its intermediate HTTP hop. Results and frontend integration are recorded under workspace .visual-review/next-trio/books. No production DB schema changes or personal data accessed.

Runtime: the previous Node20 setting is end-of-life. The release targets supported Node22 LTS (matching the local tested major), with reproducible npm ci installation. The existing Render service's NODE_VERSION/build settings must be checked before publishing.
